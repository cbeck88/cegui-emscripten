CMAKE_MINIMUM_REQUIRED(VERSION 2.8.12)
set(SAMPLE_NAME "CEGUITreeDemo")

PROJECT(${SAMPLE_NAME})

MESSAGE(STATUS "COMMON INCLUDE" ${CEGUI_SAMPLES_COMMON_INCLUDE})


CreateLibrary(${SAMPLE_NAME}
DIRS
    .
INCLUDES
    PUBLIC
        .
    PRIVATE
    ${CEGUI_SAMPLES_COMMON_INCLUDE}
        ${CEGUI_INCLUDE_DIRS}
PROPERTIES
    FOLDER "3rd Party"

)

set(CEGUI_SAMPLES ${CEGUI_SAMPLES} " " ${SAMPLE_NAME} PARENT_SCOPE)
